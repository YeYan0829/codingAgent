# README 可读性检查

检查日期：2026-09-08。对象是本工作区当前的 `README.md`。

本检查不验证产品功能。技术事实仍以代码、测试和发布验收记录为准。

## 检查方法

逐节记录新概念数量，并检查内部术语是否先解释后使用。

正文句子按标点拆分后检查长度。代码块、命令、URL、表格和 Mermaid 标签不计入自然语言长句。

Engineering Highlights 另检查四段顺序：具体问题、设计选择、得到的保证、能力边界。

## 逐节结果

| README 部分 | 新增核心概念 | 未解释术语 | 超过两个主要事实的长句 | 可删除的实现细节 | 一句话复述 |
| --- | --- | --- | --- | --- | --- |
| 开篇 | 4：产品定位、隔离修改、交付选择、两部分组成 | 无；worktree、Accept、Discard 和 Runtime 均用普通语言说明 | 无 | 已移除发布素材占位和内部状态 | CodeAgent 在 VS Code 中隔离改代码，最后由用户决定是否应用。 |
| 为什么做 CodeAgent | 3：命令权限、上下文增长、测试与代码的关系 | 无 | 无 | 已移除事件和状态结构 | 项目解决的是 Agent 如何安全、持续并可审查地完成真实任务。 |
| 一次任务如何完成 | 5：任务流程、仓库前提、额外权限、模型调用上限、SWE-bench | 无；SWE-bench 在此用普通语言解释 | 无 | 图中只保留用户可见步骤 | 任务从只读探索进入隔离修改，最后经过 Diff 和测试审查。 |
| Highlight 1 | 1：Bubblewrap 命令隔离；worktree 和审批已在前文出现 | 无 | 无 | 挂载参数和权限对象已移至安全文档 | Agent 能运行项目命令，但默认没有宿主写权限和网络。 |
| Highlight 2 | 1：每次模型调用前重建上下文 | 无 | 无 | 裁剪顺序、事件投影和 token 算法已移至架构文档 | 长任务会缩减旧工具输出，并持续提供当前工作区事实。 |
| Highlight 3 | 1：测试结果与当时的代码绑定 | 无 | 无 | revision、tree 和 evidence 结构已移至产品与架构文档 | 旧测试结果不会被当作当前代码的通过证明。 |
| Highlight 4 | 1：可观察且可干预的执行；时间线和预算已在前文出现 | 无 | 无 | 内部 job、事件和 Stop 状态机已移至产品文档 | 用户能看见任务停在哪里，并决定停止或继续。 |
| Highlight 5 | 1：可复查的评测运行 | 无；grader 在首次出现时解释为官方评分程序 | 无 | manifest、fingerprint 和重试字段已移至评测文档 | 每个评测数字都应能追溯到固定环境和明确运行记录。 |
| Quick Start：安装 | 2：系统依赖、源码虚拟环境 | 无；Runtime 已在开篇解释 | 无；长文本只出现在命令中 | 命令均是完成安装所需信息 | 在 Linux 或 WSL2 中建立虚拟环境并安装源码版本。 |
| Quick Start：验证 | 1：Fake provider | 无；首次出现时解释为固定响应程序 | 无；长文本只出现在命令中 | 保留与真实模型的区别，避免误解 | 用一个无需网络的固定响应检查 Runtime 是否可用。 |
| Quick Start：VS Code | 2：演示仓库、Extension Development Host | 无；Development Host 在首次出现时解释 | 无；长文本只出现在路径和命令中 | 保留完整可执行路径 | 打开演示仓库，完成一次修改、测试和 Diff 审查。 |
| 架构概览 | 2：本地 JSON-RPC、评测 Docker 路径 | 无；其余通用词已在前文出现 | 无 | 模块类名和内部状态已删除 | VS Code 连接本地 Runtime，工具在 worktree 和 sandbox 中运行。 |
| 示例与评测 | 3：示例目录、正式评测状态、展示素材清单 | 无 | 无 | 没有账本或数据结构细节 | 示例用于复现产品流程，截图方案与正式评测状态都有明确入口。 |
| 当前限制 | 1：当前支持边界汇总 | 无 | 无 | 只保留影响使用决策的限制 | 当前产品只支持特定平台，并明确缺少哪些安全与自动化能力。 |
| 文档 | 1：文档导航 | 无 | 无 | 每项只保留读者问题 | 读者可以按自己的问题进入对应文档。 |
| License | 0 | 无 | 无 | 无 | 项目使用 MIT License。 |

## 自动检查结果

- 五个 Engineering Highlight 均为四个自然段，顺序符合固定模板。
- README 没有出现 Candidate、validation evidence、baseline、identity、fingerprint、UserTurn、ModelStep、ToolExchange、manifest、current 或 stale。
- 排除代码、命令、URL、表格和图标签后，没有超过 35 个字符仍继续堆叠并列关系的句子。
- README 的本地链接均指向存在的文件。
- `git diff --check` 通过。

## 人话复述测试

五个亮点可以分别复述为：

1. 它能跑真实命令，但不会默认拿到整个宿主机权限。
2. 它会控制长任务的历史大小，并持续告诉模型当前代码状态。
3. 它不会拿旧测试结果证明新代码已经通过。
4. 它会展示任务为什么停住，并允许用户停止或继续。
5. 它会记录评测条件，让数字可以被复查。

这些复述都描述了要解决的问题和得到的行为，没有依赖内部对象名称。
